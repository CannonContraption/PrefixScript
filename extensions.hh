class extensions{
protected:
    commandparser parser;
    double extensionargument;
public:
    int runfile(string filename);
    double extensionreturncode;
    double getreturn();
    void passargument(double argument);
};
